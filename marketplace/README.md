# AI Learning Marketplace — MVP Frontend

This is a small Vite + React frontend scaffold for the AI Learning Marketplace MVP.

Quick start:

```bash
cd marketplace
npm install
npm run dev
```

Build:

```bash
npm run build
```

This scaffold provides a landing page, product grid, and basic styles. Connect to the Express API stub at `../marketplace-server` for product data.
